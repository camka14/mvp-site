# Docker Compose reads `.env` by default and performs variable interpolation,
# which can produce warnings if your app secrets contain `$`.
# Use this file via `docker compose --env-file .env.docker ...` to keep Compose
# focused on database settings only.

POSTGRES_PORT=5433

