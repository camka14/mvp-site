/** Operator-approved setup for the New York Empire Baseball stored-intake club package. */
import dotenv from 'dotenv';
import fs from 'node:fs/promises';
import path from 'node:path';
import sharp from 'sharp';
import type { AffiliateScrapeMapping } from '../src/server/affiliateImports/types';
import { NEW_YORK_EMPIRE_HOME_URL, NEW_YORK_EMPIRE_LOGO_SOURCE_URL, NEW_YORK_EMPIRE_MAPPING, NEW_YORK_EMPIRE_ORG_DESCRIPTION, NEW_YORK_EMPIRE_SOURCE_EVIDENCE, NEW_YORK_EMPIRE_STATIC_PAGE_CLIENT, NEW_YORK_EMPIRE_TRAVEL_URL } from '../src/server/affiliateImports/newYorkEmpireBaseballSource';

dotenv.config({ quiet: true });
dotenv.config({ path: path.join(process.cwd(), '.env.local'), override: false, quiet: true });
if (process.argv.includes('--live')) {
  const liveDatabaseUrl = process.env.DATABASE_URL_LIVE?.trim();
  if (!liveDatabaseUrl) throw new Error('DATABASE_URL_LIVE is required with --live.');
  process.env.DATABASE_URL = liveDatabaseUrl;
  process.env.PG_SSL_REJECT_UNAUTHORIZED = 'false';
  process.env.STORAGE_PROVIDER = 'spaces';
}

const OWNER_EMAIL = 'samuel.r@razumly.com';
const ORG_ID = 'affiliate_org_new_york_empire_baseball';
const SOURCE_ID = 'affiliate_source_new_york_empire_baseball';
const SOURCE_KEY = 'new-york-new-york-metropolitan-area-competitive-travel-baseball-newyorkempirebaseball-org';
const MAPPING_ID = 'affiliate_mapping_new_york_empire_baseball_v1';
const LOGO_FILE_ID = 'affiliate_file_new_york_empire_baseball_logo';
const LOGO_PATH = path.join(process.cwd(), 'src/server/affiliateImports/fixtures/newYorkEmpireBaseballLogo.png');

const sourceMetadata = { sourceEvidence: NEW_YORK_EMPIRE_SOURCE_EVIDENCE, inspectedAt: NEW_YORK_EMPIRE_SOURCE_EVIDENCE.capturedAt, robotsUrl: 'https://newyorkempirebaseball.org/robots.txt', robotsAllowed: true, robotsNote: 'The stored New York Empire travel-baseball listing is ALLOWED. Facility, location, program, camp, and other detail pages are UNCHECKED and remain withheld.', reviewedUrls: [NEW_YORK_EMPIRE_TRAVEL_URL], officialActionUrls: [NEW_YORK_EMPIRE_HOME_URL, NEW_YORK_EMPIRE_TRAVEL_URL], officialLogoSourceUrl: NEW_YORK_EMPIRE_LOGO_SOURCE_URL, officialLogoCandidateArtifact: 'output/affiliate-intakes/new-york-new-york-metropolitan-area-competitive-travel-baseball-newyorkempirebaseball-org/1b23cdf3-cb7e-432c-972f-db3f0e008ff1/002-logo_candidate-7eaff9e5-8faf-4053-9c8c-af388ea67591.png', logoStatus: 'VERIFIED_OFFICIAL', logoSourceType: 'Stored first-party New York Empire Baseball page-branding/logo candidate 002', logoNormalizedFormat: 'opaque-1024-square-png', logoBackground: '#111827', logoNote: 'The stored first-party New York Empire Baseball wordmark was flattened onto a dark opaque 1024px square PNG without changing the mark.', logoDisposition: 'OFFICIAL_ASSET', cadence: 'weekly', cadenceIntervalMinutes: 10080, withheldRows: [{ title: 'New York Empire current travel-team rows', reason: 'The stored allowed listing provides evergreen travel-program context but no complete current date, time, venue, price, or registration row.' }, { title: 'New York Empire facility and program details', reason: 'The stored facility, locations, program, camp, and other detail pages are UNCHECKED.' }, { title: 'New York Empire teams and rosters', reason: 'TEAM mappings are out of scope.' }] };

const streamToBuffer = async (stream: NodeJS.ReadableStream): Promise<Buffer> => new Promise((resolve, reject) => { const chunks: Buffer[] = []; stream.on('data', (chunk) => chunks.push(Buffer.from(chunk))); stream.on('error', reject); stream.on('end', () => resolve(Buffer.concat(chunks))); });
const upsertLogo = async (prisma: any, ownerId: string) => { const data = await fs.readFile(LOGO_PATH); const metadata = await sharp(data).metadata(); if (metadata.width !== 1024 || metadata.height !== 1024 || metadata.hasAlpha) throw new Error('New York Empire logo must be opaque 1024x1024 PNG.'); const { getStorageProvider } = await import('../src/lib/storageProvider'); const storage = getStorageProvider(); const existing = await prisma.file.findUnique({ where: { id: LOGO_FILE_ID }, select: { path: true, bucket: true } }); let stored: any = null; if (existing?.path) { try { const object = await storage.getObjectStream({ key: existing.path, bucket: existing.bucket }); if ((await streamToBuffer(object.stream)).equals(data)) stored = { key: existing.path, sizeBytes: data.length, bucket: existing.bucket ?? undefined }; } catch {} } if (!stored) stored = await storage.putObject({ data, originalName: 'new-york-empire-baseball-logo-square.png', contentType: 'image/png', organizationId: ORG_ID }); await prisma.file.upsert({ where: { id: LOGO_FILE_ID }, create: { id: LOGO_FILE_ID, uploaderId: ownerId, organizationId: ORG_ID, bucket: stored.bucket ?? null, originalName: 'new-york-empire-baseball-logo-square.png', mimeType: 'image/png', sizeBytes: stored.sizeBytes, path: stored.key, createdAt: new Date(), updatedAt: new Date() }, update: { uploaderId: ownerId, organizationId: ORG_ID, bucket: stored.bucket ?? null, originalName: 'new-york-empire-baseball-logo-square.png', mimeType: 'image/png', sizeBytes: stored.sizeBytes, path: stored.key, updatedAt: new Date() } }); return LOGO_FILE_ID; };

const main = async () => { const prisma = (await import('../src/lib/prisma')).prisma; const runAffiliateSourceScrape = (await import('../src/server/affiliateImports/service')).runAffiliateSourceScrape; try { const owner = await prisma.authUser.findUnique({ where: { email: OWNER_EMAIL }, select: { id: true } }); if (!owner?.id) throw new Error(`Owner user ${OWNER_EMAIL} was not found.`); const logoId = await upsertLogo(prisma, owner.id); const organization = { updatedAt: new Date(), name: 'New York Empire Baseball', location: 'New York, NY', address: '251 West 60 Street between West End Avenue & Amsterdam Avenue, New York, NY', description: NEW_YORK_EMPIRE_ORG_DESCRIPTION, logoId, ownerId: owner.id, website: NEW_YORK_EMPIRE_HOME_URL, sports: ['Baseball'], status: 'UNLISTED' as const, publicPageEnabled: false, publicWidgetsEnabled: false }; await prisma.organizations.upsert({ where: { id: ORG_ID }, create: { id: ORG_ID, createdAt: new Date(), hasStripeAccount: false, verificationStatus: 'UNVERIFIED', verificationReviewStatus: 'NONE', ...organization }, update: organization }); const source = { name: 'New York Empire Baseball', sourceKey: SOURCE_KEY, organizationId: ORG_ID, baseUrl: NEW_YORK_EMPIRE_HOME_URL, listUrl: NEW_YORK_EMPIRE_TRAVEL_URL, targetKind: 'CLUB', status: 'ACTIVE', autoScrapeEnabled: false, scrapeIntervalMinutes: 10080, notes: 'Stored-intake New York Empire Baseball club package with one ongoing CLUB profile; unchecked program and team rows remain withheld.', metadata: sourceMetadata }; await prisma.affiliateScrapeSources.upsert({ where: { id: SOURCE_ID }, create: { id: SOURCE_ID, ...source }, update: source }); await prisma.affiliateScrapeMappings.updateMany({ where: { sourceId: SOURCE_ID }, data: { isActive: false } }); await prisma.affiliateScrapeMappings.upsert({ where: { id: MAPPING_ID }, create: { id: MAPPING_ID, sourceId: SOURCE_ID, version: 1, isActive: true, mapping: NEW_YORK_EMPIRE_MAPPING satisfies AffiliateScrapeMapping, createdByUserId: null, notes: 'Review-only CLUB mapping from stored New York Empire Baseball evidence.', validatedAt: null }, update: { version: 1, isActive: true, mapping: NEW_YORK_EMPIRE_MAPPING satisfies AffiliateScrapeMapping, notes: 'Review-only CLUB mapping from stored New York Empire Baseball evidence.', validatedAt: null } }); await prisma.affiliateScrapeSources.update({ where: { id: SOURCE_ID }, data: { activeMappingId: MAPPING_ID, autoScrapeEnabled: false } }); console.log(`Affiliate source ready for local review: ${SOURCE_KEY}`); if (process.argv.includes('--scrape')) { const result = await runAffiliateSourceScrape(SOURCE_ID, { importMode: 'REVIEW', client: NEW_YORK_EMPIRE_STATIC_PAGE_CLIENT }); const logs = result.run.logs as Record<string, unknown> | null; console.log(JSON.stringify({ runId: result.run.id, candidateCount: result.candidates.length, normalizedCandidates: result.candidates.map((candidate: any) => ({ listingKind: candidate.listingKind, title: candidate.title, officialActionUrl: candidate.officialActionUrl, dateDisplayMode: candidate.dateDisplayMode })), createdCandidateCount: logs?.createdCandidateCount ?? null, updatedCandidateCount: logs?.updatedCandidateCount ?? null, rejectedCount: logs?.rejectedCount ?? null }, null, 2)); } } finally { await prisma.$disconnect(); } };
main().catch((error) => { console.error('[setup-new-york-empire-baseball-affiliate-source] failed', error); process.exitCode = 1; });
