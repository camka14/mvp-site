/** Operator-approved setup for the Sofive Brooklyn stored-intake rental package. */
import dotenv from 'dotenv';
import fs from 'node:fs/promises';
import path from 'node:path';
import sharp from 'sharp';
import type { AffiliateScrapeMapping } from '../src/server/affiliateImports/types';
import { SOFIVE_BROOKLYN_HOME_URL, SOFIVE_BROOKLYN_LOGO_SOURCE_URL, SOFIVE_BROOKLYN_MAPPING, SOFIVE_BROOKLYN_SOURCE_EVIDENCE, SOFIVE_BROOKLYN_SOURCE_URL, SOFIVE_BROOKLYN_STATIC_PAGE_CLIENT } from '../src/server/affiliateImports/sofiveBrooklynSource';

dotenv.config({ quiet: true });
dotenv.config({ path: path.join(process.cwd(), '.env.local'), override: false, quiet: true });
if (process.argv.includes('--live')) {
  const liveDatabaseUrl = process.env.DATABASE_URL_LIVE?.trim();
  if (!liveDatabaseUrl) throw new Error('DATABASE_URL_LIVE is required with --live.');
  process.env.DATABASE_URL = liveDatabaseUrl;
  process.env.PG_SSL_REJECT_UNAUTHORIZED = 'false';
  process.env.STORAGE_PROVIDER = 'spaces';
}

const OWNER_EMAIL = 'samuel.r@razumly.com';
const ORG_ID = 'affiliate_org_sofive_brooklyn';
const SOURCE_ID = 'affiliate_source_sofive_brooklyn';
const SOURCE_KEY = 'sofive-brooklyn-indoor-soccer-rentals';
const MAPPING_ID = 'affiliate_mapping_sofive_brooklyn_v1';
const LOGO_FILE_ID = 'affiliate_file_sofive_brooklyn_logo';
const LOGO_PATH = path.join(process.cwd(), 'src/server/affiliateImports/fixtures/sofiveBrooklynLogo.png');
const streamToBuffer = async (stream: NodeJS.ReadableStream): Promise<Buffer> => new Promise((resolve, reject) => { const chunks: Buffer[] = []; stream.on('data', (chunk) => chunks.push(Buffer.from(chunk))); stream.on('error', reject); stream.on('end', () => resolve(Buffer.concat(chunks))); });

const sourceMetadata = { sourceEvidence: SOFIVE_BROOKLYN_SOURCE_EVIDENCE, inspectedAt: SOFIVE_BROOKLYN_SOURCE_EVIDENCE.capturedAt, robotsUrl: 'https://www.sofive.com/robots.txt', robotsAllowed: true, robotsNote: 'The stored Brooklyn rental path is ALLOWED; linked location and program pages are UNCHECKED discovery evidence and are not blindly fetched.', reviewedUrls: [SOFIVE_BROOKLYN_SOURCE_URL], officialActionUrls: [SOFIVE_BROOKLYN_SOURCE_URL], officialLogoSourceUrl: SOFIVE_BROOKLYN_LOGO_SOURCE_URL, logoStatus: 'VERIFIED_OFFICIAL', logoSourceType: 'Stored official Sofive JSON-LD organization logo candidate', logoNormalizedFormat: 'opaque-1024-square-png', logoBackground: '#ffffff', logoNote: 'The stored first-party Sofive SVG was rendered and normalized to an opaque 1024px square PNG.', logoDisposition: 'OFFICIAL_ASSET', cadence: 'monthly', cadenceIntervalMinutes: 43200, withheldRows: [{ title: 'Brooklyn location details', reason: 'The location page was discovered but not captured with complete facility details.' }, { title: 'Brooklyn tournaments and programs', reason: 'The partial capture does not supply complete current rows; no EVENT or TEAM candidate is created.' }] };

const upsertLogo = async (prisma: any, ownerId: string) => {
  const data = await fs.readFile(LOGO_PATH); const metadata = await sharp(data).metadata(); if (metadata.width !== 1024 || metadata.height !== 1024 || metadata.hasAlpha) throw new Error('Sofive Brooklyn logo must be opaque 1024x1024 PNG.'); const { getStorageProvider } = await import('../src/lib/storageProvider'); const storage = getStorageProvider(); const existing = await prisma.file.findUnique({ where: { id: LOGO_FILE_ID }, select: { path: true, bucket: true } }); let stored: any = null; if (existing?.path) { try { const object = await storage.getObjectStream({ key: existing.path, bucket: existing.bucket }); if ((await streamToBuffer(object.stream)).equals(data)) stored = { key: existing.path, sizeBytes: data.length, bucket: existing.bucket ?? undefined }; } catch {} } if (!stored) stored = await storage.putObject({ data, originalName: 'sofive-brooklyn-logo-square.png', contentType: 'image/png', organizationId: ORG_ID }); await prisma.file.upsert({ where: { id: LOGO_FILE_ID }, create: { id: LOGO_FILE_ID, uploaderId: ownerId, organizationId: ORG_ID, bucket: stored.bucket ?? null, originalName: 'sofive-brooklyn-logo-square.png', mimeType: 'image/png', sizeBytes: stored.sizeBytes, path: stored.key, createdAt: new Date(), updatedAt: new Date() }, update: { uploaderId: ownerId, organizationId: ORG_ID, bucket: stored.bucket ?? null, originalName: 'sofive-brooklyn-logo-square.png', mimeType: 'image/png', sizeBytes: stored.sizeBytes, path: stored.key, updatedAt: new Date() } }); return LOGO_FILE_ID;
};

const main = async () => { const prisma = (await import('../src/lib/prisma')).prisma; const runAffiliateSourceScrape = (await import('../src/server/affiliateImports/service')).runAffiliateSourceScrape; try { const owner = await prisma.authUser.findUnique({ where: { email: OWNER_EMAIL }, select: { id: true } }); if (!owner?.id) throw new Error(`Owner user ${OWNER_EMAIL} was not found.`); const logoId = await upsertLogo(prisma, owner.id); const organization = { updatedAt: new Date(), name: 'Sofive Brooklyn', location: 'Brooklyn, NY', address: null, description: 'The stored Sofive intake identifies an official Brooklyn indoor soccer field-rental path; complete facility details require review of the official rental page.', logoId, ownerId: owner.id, website: SOFIVE_BROOKLYN_HOME_URL, sports: ['Soccer'], status: 'UNLISTED' as const, publicPageEnabled: false, publicWidgetsEnabled: false }; await prisma.organizations.upsert({ where: { id: ORG_ID }, create: { id: ORG_ID, createdAt: new Date(), hasStripeAccount: false, verificationStatus: 'UNVERIFIED', verificationReviewStatus: 'NONE', ...organization }, update: organization }); const source = { name: 'Sofive Brooklyn Indoor Soccer Field Rental', sourceKey: SOURCE_KEY, organizationId: ORG_ID, baseUrl: SOFIVE_BROOKLYN_HOME_URL, listUrl: SOFIVE_BROOKLYN_SOURCE_URL, targetKind: 'RENTAL', status: 'ACTIVE', autoScrapeEnabled: false, scrapeIntervalMinutes: 43200, notes: 'Stored-intake partial Brooklyn rental path; no price, availability, address, or current event rows are inferred.', metadata: sourceMetadata }; await prisma.affiliateScrapeSources.upsert({ where: { id: SOURCE_ID }, create: { id: SOURCE_ID, ...source }, update: source }); await prisma.affiliateScrapeMappings.updateMany({ where: { sourceId: SOURCE_ID }, data: { isActive: false } }); await prisma.affiliateScrapeMappings.upsert({ where: { id: MAPPING_ID }, create: { id: MAPPING_ID, sourceId: SOURCE_ID, version: 1, isActive: true, mapping: SOFIVE_BROOKLYN_MAPPING satisfies AffiliateScrapeMapping, createdByUserId: null, notes: 'Review-only RENTAL mapping from stored Sofive Brooklyn evidence.', validatedAt: null }, update: { version: 1, isActive: true, mapping: SOFIVE_BROOKLYN_MAPPING satisfies AffiliateScrapeMapping, notes: 'Review-only RENTAL mapping from stored Sofive Brooklyn evidence.', validatedAt: null } }); await prisma.affiliateScrapeSources.update({ where: { id: SOURCE_ID }, data: { activeMappingId: MAPPING_ID, autoScrapeEnabled: false } }); console.log(`Affiliate source ready for local review: ${SOURCE_KEY}`); if (process.argv.includes('--scrape')) { const result = await runAffiliateSourceScrape(SOURCE_ID, { importMode: 'REVIEW', client: SOFIVE_BROOKLYN_STATIC_PAGE_CLIENT }); const logs = result.run.logs as any; console.log(JSON.stringify({ runId: result.run.id, candidateCount: result.candidates.length, normalizedCandidates: result.candidates.map((candidate: any) => ({ listingKind: candidate.listingKind, title: candidate.title, officialActionUrl: candidate.officialActionUrl, dateDisplayMode: candidate.dateDisplayMode })), createdCandidateCount: logs?.createdCandidateCount ?? null, updatedCandidateCount: logs?.updatedCandidateCount ?? null, rejectedCount: logs?.rejectedCount ?? null }, null, 2)); } } finally { await prisma.$disconnect(); } };
main().catch((error) => { console.error('[setup-sofive-brooklyn-affiliate-source] failed', error); process.exitCode = 1; });
