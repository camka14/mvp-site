# Copy to migration.env on the VM and set mode 0600.
# This uses the database owner and must never be given to the long-running app.
DATABASE_URL=postgresql://bracketiq_owner:URL_ENCODED_PASSWORD@postgres:5432/bracketiq
