# Copy to postgres.env on the VM and set mode 0600.
POSTGRES_DB=bracketiq
POSTGRES_USER=bracketiq_owner
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_OWNER_PASSWORD
POSTGRES_RUNTIME_USER=bracketiq_app
POSTGRES_RUNTIME_PASSWORD=REPLACE_WITH_RANDOM_RUNTIME_PASSWORD
