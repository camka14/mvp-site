# The model service intentionally has no env_file in compose.yml.
# llama-server configuration is supplied through deployment.env and the only
# secret is mounted as /run/secrets/model_api_key.
#
# Never add DATABASE_URL, object-storage credentials, GitHub credentials,
# Codex credentials, email credentials, provider API keys, or deployment keys
# to the model container.
